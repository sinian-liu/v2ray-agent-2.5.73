{
  "routing": {
    "domainStrategy": "IPOnDemand",
    "rules": [
      {
        "type": "field",
        "outboundTag": "blackhole-out",
        "protocol": [
          "bittorrent"
        ]
      },
      {
        "type": "field",
        "outboundTag": "blackhole-out",
        "protocol": [
          "bittorrent"
        ]
      }
    ]
  }
}
